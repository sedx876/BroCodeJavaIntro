
public class VariableScope {
	
	//local variable= declared inside a method visible only to that method
	//global varaible= declared outside a method, but within a class visible to all parts of the class
	
	VariableScopeDice diceRoller = new VariableScopeDice();
}
